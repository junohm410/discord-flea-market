.comment-container
  h2.text-base.font-semibold.text-gray-900.mb-4.flex.items-center.gap-2
    | 💬
    | コメント
    span.text-sm.font-normal.text-gray-600#comments_count
      | (#{item.comments.size})

  ul.space-y-3.list-none#comments
    - if (comments = item.comments.order(:id).includes(:user).presence)
      = render partial: 'comments/comment', collection: comments, as: :comment
    - else
      p.text-sm.text-gray-500.py-4.text-center まだコメントはありません

  .mt-4.pt-4.border-t.border-gray-200
    = render 'comments/form', comment: item.comments.build
