.comment-container
  h2.my-5.text-xl.font-medium
    | コメント(
    span#comments_count = item.comments.count
    | )
  ul#comments
    - if (comments = item.comments.order(:id).includes(:user).presence)
      = render partial: 'comments/comment', collection: comments, as: :comment, locals: { item: }

  = render 'comments/form', comment: item.comments.build
